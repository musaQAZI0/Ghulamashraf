import { Quote } from "lucide-react";

export function QuoteSection() {
  return (
    <section className="quote-section">
      <Quote size={34} />
      <p>
        True learning becomes meaningful when it refines character, enlarges responsibility, and returns to society as
        service.
      </p>
    </section>
  );
}
